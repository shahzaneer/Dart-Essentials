main() {
  // dart is a type safe language
  // declaring types in dart is mandatory
  // dart uses the concept of type inference for this purpose if the type is not declared

  var sh = 9; // dart k static type check analyzer iska ta'ayyun khud krlega
  // weakly typed + Strongly typed -> dono hi languages ko support krta hai yeh
  print(sh.runtimeType);
}
